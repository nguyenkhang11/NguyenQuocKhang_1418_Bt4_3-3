spring.application.name=chieu3_3
server.port=8081
# serve static resources from classpath:/static/ (default)
spring.web.resources.static-locations=classpath:/static/
# static-path-pattern left as default so that resources under /images/** are
# accessible without needing the /static/ prefix
